#include "main.h"

/**
 * print_alphabet_x10 - print in lowercase
 */

void print_alphabet_x10(void)
{
	int count;
	char d;

	for (count = 0; count <= 9 count++)
	{
		for (d = 'a'; d <= 'z'; d++)
			_putchar(d);

		_putchar('\n');
	}
}
